name: notes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:   # ← paket localization bawaan Flutter
    sdk: flutter
  intl: any                # ← paket format teks internasional
  shared_preferences: ^2.0.15

  # ... dependency lainnya tetap ada di sini
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0
  image_picker: ^1.2.1
  firebase_messaging: ^16.2.2
  flutter_local_notifications: ^21.0.0
  http: ^1.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
